<div class="dashboard">
  <h3>Welcome <%= current_user.uid %>!</h4>

  <h5>Currently Playing</h5>
  <div class="currently-playing">
    <% if @song %>
      <%= render partial: "currently-playing" %>
    <% else %>
      <%= "Play a song on Spotify!" %>
    <% end %>
  </div>
</div>
